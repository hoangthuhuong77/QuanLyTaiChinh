import { useState } from 'react';import { Alert,KeyboardAvoidingView,Platform,Text,View } from 'react-native';import { Link } from 'expo-router';import { Button,Card,Field,Screen } from '@/thanhphan/dungchung/UI';import { Colors } from '@/hangso/colors';import { useAuth } from '@/ngucanh/AuthContext';import { apiMessage } from '@/tienich/dinhdang';
export default function Login(){const [login,setLogin]=useState('');const [password,setPassword]=useState('');const [loading,setLoading]=useState(false);const {login:signIn}=useAuth();const submit=async()=>{if(!login.trim()||!password)return Alert.alert('Thiếu thông tin','Vui lòng nhập tên đăng nhập và mật khẩu.');try{setLoading(true);await signIn(login.trim(),password);}catch(e){Alert.alert('Đăng nhập thất bại',apiMessage(e));}finally{setLoading(false)}};return <KeyboardAvoidingView style={{flex:1}} behavior={Platform.OS==='ios'?'padding':undefined}><Screen><View style={{paddingTop:45,gap:10}}><View style={{width:58,height:58,borderRadius:18,backgroundColor:Colors.primary,alignItems:'center',justifyContent:'center'}}><Text style={{fontSize:28}}>₫</Text></View><Text style={{fontSize:32,fontWeight:'900',color:Colors.text}}>Tài chính trong tầm tay</Text><Text style={{color:Colors.muted,fontSize:16}}>Đăng nhập để tiếp tục quản lý dòng tiền của bạn.</Text></View><Card style={{gap:16}}><Field label="Email hoặc tên đăng nhập" value={login} onChangeText={setLogin} placeholder="huong@example.com"/><Field label="Mật khẩu" value={password} onChangeText={setPassword} secureTextEntry placeholder="Ít nhất 6 ký tự"/><Button title="Đăng nhập" onPress={submit} loading={loading}/><Text style={{textAlign:'center',color:Colors.muted}}>Chưa có tài khoản? <Link href="/(auth)/dangky" style={{color:Colors.primary,fontWeight:'800'}}>Đăng ký</Link></Text></Card></Screen></KeyboardAvoidingView>}

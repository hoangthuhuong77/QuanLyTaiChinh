import { useState } from 'react';import { Alert,Text } from 'react-native';import { Link } from 'expo-router';import { Button,Card,Field,Header,Screen } from '@/thanhphan/dungchung/UI';import { Colors } from '@/hangso/colors';import { useAuth } from '@/ngucanh/AuthContext';import { apiMessage } from '@/tienich/dinhdang';
export default function Register(){const [form,setForm]=useState({full_name:'',email:'',username:'',phone:'',password:''});const [loading,setLoading]=useState(false);const {register}=useAuth();const field=(key:keyof typeof form)=>(v:string)=>setForm(x=>({...x,[key]:v}));const submit=async()=>{if(!form.full_name||!form.email.includes('@')||!form.username||form.password.length<6)return Alert.alert('Dữ liệu chưa hợp lệ','Nhập đủ thông tin, email hợp lệ và mật khẩu ít nhất 6 ký tự.');try{setLoading(true);await register(form);}catch(e){Alert.alert('Đăng ký thất bại',apiMessage(e));}finally{setLoading(false)}};return <Screen><Header title="Tạo tài khoản" subtitle="Bắt đầu xây dựng thói quen tài chính tốt hơn."/><Card style={{gap:14}}><Field label="Họ và tên" value={form.full_name} onChangeText={field('full_name')} placeholder="Nguyễn Thu Hương"/><Field label="Email" value={form.email} onChangeText={field('email')} keyboardType="email-address" placeholder="huong@example.com"/><Field label="Tên đăng nhập" value={form.username} onChangeText={field('username')} placeholder="thuhuong"/><Field label="Số điện thoại (không bắt buộc)" value={form.phone} onChangeText={field('phone')} keyboardType="phone-pad" placeholder="090..."/><Field label="Mật khẩu" value={form.password} onChangeText={field('password')} secureTextEntry placeholder="Ít nhất 6 ký tự"/><Button title="Đăng ký" onPress={submit} loading={loading}/><Text style={{textAlign:'center',color:Colors.muted}}>Đã có tài khoản? <Link href="/(auth)/dangnhap" style={{color:Colors.primary,fontWeight:'800'}}>Đăng nhập</Link></Text></Card></Screen>}

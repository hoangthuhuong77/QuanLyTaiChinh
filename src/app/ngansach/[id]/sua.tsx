import { useEffect,useState } from 'react';import { useLocalSearchParams } from 'expo-router';import type { Budget } from '@/kieudulieu';import { budgetService } from '@/dichvu';import { Header,Loading,Screen } from '@/thanhphan/dungchung/UI';import { BudgetForm } from '@/thanhphan/ngansach/BudgetForm';export default function Edit(){const {id}=useLocalSearchParams<{id:string}>();const [item,setItem]=useState<Budget>();useEffect(()=>{budgetService.list().then(r=>setItem(r.data.find(x=>String(x.id)===id)))},[id]);return <Screen><Header title="Sửa ngân sách"/>{item?<BudgetForm initial={item}/>:<Loading/>}</Screen>}

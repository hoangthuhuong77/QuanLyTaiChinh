import { useEffect,useState } from 'react';import { useLocalSearchParams } from 'expo-router';import { Header,Loading,Screen } from '@/thanhphan/dungchung/UI';import { transactionService } from '@/dichvu';import type { Transaction } from '@/kieudulieu';import { TransactionForm } from '@/thanhphan/giaodich/TransactionForm';export default function Edit(){const {id}=useLocalSearchParams<{id:string}>();const [item,setItem]=useState<Transaction>();useEffect(()=>{transactionService.detail(id).then(r=>setItem(r.data))},[id]);return <Screen><Header title="Sửa giao dịch"/>{item?<TransactionForm initial={item}/>:<Loading/>}</Screen>}

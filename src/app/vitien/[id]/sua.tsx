import { useEffect,useState } from 'react';import { useLocalSearchParams } from 'expo-router';import type { Wallet } from '@/kieudulieu';import { walletService } from '@/dichvu';import { Header,Loading,Screen } from '@/thanhphan/dungchung/UI';import { WalletForm } from '@/thanhphan/vitien/WalletForm';export default function Edit(){const {id}=useLocalSearchParams<{id:string}>();const [item,setItem]=useState<Wallet>();useEffect(()=>{walletService.detail(id).then(r=>setItem(r.data))},[id]);return <Screen><Header title="Sửa ví"/>{item?<WalletForm initial={item}/>:<Loading/>}</Screen>}

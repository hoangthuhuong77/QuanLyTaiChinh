import { Pressable,Text,View } from 'react-native';import { router } from '@/tienich/router';import { Button,Card,Header,Screen } from '@/thanhphan/dungchung/UI';import { useAuth } from '@/ngucanh/AuthContext';import { Colors } from '@/hangso/colors';
const menu=[['Ví tiền','/vitien'],['Danh mục','/danhmuc'],['Ngân sách','/ngansach'],['Mục tiêu tiết kiệm','/tietkiem'],['Chỉnh sửa hồ sơ','/canhan/sua'],['Đổi mật khẩu','/canhan/doimatkhau']] as const;
export default function Profile(){const {user,logout}=useAuth();return <Screen><Header title="Cá nhân" subtitle="Tài khoản và công cụ quản lý"/><Card style={{alignItems:'center',gap:7}}><View style={{width:72,height:72,borderRadius:36,backgroundColor:Colors.primarySoft,alignItems:'center',justifyContent:'center'}}><Text style={{fontSize:30,color:Colors.primary,fontWeight:'900'}}>{user?.full_name.charAt(0)}</Text></View><Text style={{fontSize:20,fontWeight:'900',color:Colors.text}}>{user?.full_name}</Text><Text style={{color:Colors.muted}}>{user?.email}</Text></Card><Card style={{padding:4}}>{menu.map(([label,path],i)=><Pressable key={path} onPress={()=>router.push(path)} style={{padding:16,flexDirection:'row',borderBottomWidth:i===menu.length-1?0:1,borderColor:Colors.border}}><Text style={{flex:1,color:Colors.text,fontWeight:'700'}}>{label}</Text><Text style={{color:Colors.muted}}>›</Text></Pressable>)}</Card><Button title="Đăng xuất" variant="secondary" onPress={logout}/></Screen>}

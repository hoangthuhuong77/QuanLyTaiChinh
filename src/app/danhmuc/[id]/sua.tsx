import { useEffect,useState } from 'react';import { useLocalSearchParams } from 'expo-router';import type { Category } from '@/kieudulieu';import { categoryService } from '@/dichvu';import { Header,Loading,Screen } from '@/thanhphan/dungchung/UI';import { CategoryForm } from '@/thanhphan/danhmuc/CategoryForm';export default function Edit(){const {id}=useLocalSearchParams<{id:string}>();const [item,setItem]=useState<Category>();useEffect(()=>{categoryService.list().then(r=>setItem(r.data.find(x=>String(x.id)===id)))},[id]);return <Screen><Header title="Sửa danh mục"/>{item?<CategoryForm initial={item}/>:<Loading/>}</Screen>}

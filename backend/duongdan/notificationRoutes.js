const router=require('express').Router();const controller=require('../dieukhien/notificationController');router.get('/',controller.list);router.put('/read-all',controller.markAllRead);router.put('/:id/read',controller.markRead);router.delete('/:id',controller.remove);module.exports=router;
